import "./match-statistics-data.css";
import MatchSummaryHeader from "./match-summary-header";

function MatchStatisticsData({ data }) {
  return (
    <div> <MatchSummaryHeader data={data} /></div>
  );
}

export default MatchStatisticsData;
